// Copy to secrets.h and fill in. secrets.h is gitignored.
#pragma once

#define WIFI_SSID "your-network"
#define WIFI_PASS "your-password"

// The Mac running the host server. Phase 0 only prints this back for
// confirmation; Phase 1 connects to it.
#define HOST_IP   "192.168.1.100"
#define HOST_PORT 8765
